# ==========================================
# DATABASE CONFIGURATION
# ==========================================

# PostgreSQL connection string
# Format: postgres://username:password@host:port/database
# Development: localhost or postgres (in Docker)
# Production: your production database URL
DATABASE_URI=postgres://postgres:dev_password_123@localhost:5432/bluehive_website

# Alternative: separate PostgreSQL variables (for docker-compose.yml)
POSTGRES_DB=bluehive_website
POSTGRES_USER=postgres
POSTGRES_PASSWORD=dev_password_123

# ==========================================
# PAYLOAD CMS
# ==========================================

# Secret key for Payload (MUST be unique and secure in production!)
# Generate a strong random string for production
PAYLOAD_SECRET=dev-secret-key-123456789

# ==========================================
# APPLICATION
# ==========================================

# Environment: development or production
NODE_ENV=development

# Port to run the application on (default: 3000)
# Only relevant if running without Docker
PORT=3000

# Port for Docker production (default: 4000)
APP_PORT=4000

# ==========================================
# OPTIONAL: NEXT.JS
# ==========================================

# Disable telemetry (optional)
# NEXT_TELEMETRY_DISABLED=1

# Node.js options (for development)
NODE_OPTIONS=--no-deprecation

# ==========================================
# TESTING
# ==========================================

# CI environment flag (used by Playwright)
# CI=false
